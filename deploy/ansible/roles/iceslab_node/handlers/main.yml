---
- name: reload sysctl
  ansible.builtin.command: sysctl --system
  changed_when: false

- name: restart zapret2
  ansible.builtin.command:
    cmd: docker compose up -d --force-recreate
    chdir: "{{ iceslab_zapret2_dir }}"
  changed_when: true
